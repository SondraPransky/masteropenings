"""Human Opening Analytics Platform — MVP.

One base, many views. Each position is analysed once (D8); everything else is a
view or a routing of that data. See CLAUDE.md for the full architecture.
"""

__version__ = "0.1.0"
