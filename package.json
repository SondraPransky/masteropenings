{
  "name": "eecoach",
  "version": "0.1.0",
  "private": true,
  "description": "EECoach — outil pédagogique de révision d'ouvertures d'échecs. Outillage de DÉV uniquement (type-check + tests) — l'app reste sans build, chargée par <script> et déployée en statique.",
  "scripts": {
    "typecheck": "tsc -p jsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "chess.js": "^0.10.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
